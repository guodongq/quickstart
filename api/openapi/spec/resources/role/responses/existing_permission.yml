description: The response will be a JSON object with a key called `permission`. The
  value of this will be an object with the standard permission attributes

content:
  application/json:
    schema:
      allOf:
        - $ref: '../models/permission.yml#/permission'